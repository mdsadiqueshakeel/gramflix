# # name: Deploy to EC2

# # on:
# #   push:
# #     branches: [ "master" ]

# # jobs:
# #   deploy:
# #     runs-on: ubuntu-latest

# #     steps:
# #       - name: Checkout repository
# #         uses: actions/checkout@v3

# #       - name: Configure AWS credentials
# #         uses: aws-actions/configure-aws-credentials@v4
# #         with:
# #           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
# #           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
# #           aws-region: ${{ vars.AWS_REGION }}

# #       - name: Deploy App via SSM
# #         run: |
# #           INSTANCE_ID=i-003663f28cf482aa0
# #           SSM_USER_HOME=/home/ssm-user
# #           APP_DIR=$SSM_USER_HOME/app/gramflix

# #           aws ssm send-command \
# #             --targets "Key=instanceIds,Values=i-003663f28cf482aa0" \
# #             --document-name "AWS-RunShellScript" \
# #             --comment "Create app folder" \
# #             --parameters "commands=[\"mkdir -p $APP_DIR\"]"

# #           aws ssm send-command \
# #             --targets "Key=instanceIds,Values=i-003663f28cf482aa0" \
# #             --document-name "AWS-RunShellScript" \
# #             --comment "Add GitHub to known hosts" \
# #             --parameters "commands=[\"ssh-keyscan github.com >> $SSM_USER_HOME/.ssh/known_hosts\"]"

# #           aws ssm send-command \
# #             --targets "Key=instanceIds,Values=i-003663f28cf482aa0" \
# #             --document-name "AWS-RunShellScript" \
# #             --comment "Clone or update repo" \
# #             --parameters "commands=[
# #               \"if [ -d '$APP_DIR/.git' ]; then cd $APP_DIR && git pull origin master; else git clone git@github.com:mdsadiqueshakeel/gramflix.git $APP_DIR; fi\"
# #             ]"

# #           aws ssm send-command \
# #             --targets "Key=instanceIds,Values=i-003663f28cf482aa0" \
# #             --document-name "AWS-RunShellScript" \
# #             --comment "Docker refresh" \
# #             --parameters "commands=[
# #               \"cd $APP_DIR && docker-compose -f docker-compose.prod.yml pull && docker-compose -f docker-compose.prod.yml up -d --build\"
# #             ]"




# name: Build & Deploy to EC2 (t3.small safe)

# on:
#   push:
#     branches: [ "master" ]

# jobs:
#   build:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Set up Java 17
#         uses: actions/setup-java@v3
#         with:
#           java-version: '17'
#           distribution: 'temurin'

#       - name: Build Spring Boot backend JAR
#         run: |
#           cd referral-wallet-system
#           chmod +x mvnw
#           ./mvnw clean package -DskipTests

#       - name: Upload JAR to EC2
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           source: "referral-wallet-system/target/*.jar"
#           target: "/home/ec2-user/"

#       - name: Deploy on EC2
#         uses: appleboy/ssh-action@v0.1.10
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             sudo su
#             mv /home/ec2-user/*.jar /root/gramflix/referral-wallet-system/target/
#             cd /root/gramflix
#             docker compose -f docker-compose.prod.yml build backend
#             docker compose -f docker-compose.prod.yml up -d



# name: Build & Deploy to EC2

# on:
#   push:
#     branches: [ "master" ]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       # 1️⃣ Checkout code
#       - name: Checkout code
#         uses: actions/checkout@v3

#       # 2️⃣ Setup Java for building backend JAR
#       - name: Set up Java 17
#         uses: actions/setup-java@v3
#         with:
#           java-version: '17'
#           distribution: 'temurin'

#       # 3️⃣ Build Spring Boot JAR (on GitHub runner)
#       - name: Build Backend JAR
#         run: |
#           echo "🔨 Building JAR..."
#           cd referral-wallet-system
#           chmod +x mvnw
#           ./mvnw clean package -DskipTests
#           echo "📦 Built files in target/:"
#           ls -la target

#       # 4️⃣ Upload JAR to EC2 (clean target first)
#       - name: Upload JAR to EC2
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           source: "referral-wallet-system/target/*.jar"
#           target: "/home/ec2-user/gramflix/referral-wallet-system/"

#       # 5️⃣ Deploy on EC2 using Docker
#       - name: Deploy on EC2
#         uses: appleboy/ssh-action@v0.1.10
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             echo "🚀 Starting Deployment..."

#             cd /home/ec2-user/gramflix

#             echo "📦 Files inside backend target:"
#             ls referral-wallet-system/target/

#             echo "🐳 Building backend image..."
#             docker compose -f docker-compose.prod.yml build backend

#             echo "⬆️ Starting containers..."
#             docker compose -f docker-compose.prod.yml up -d

#             echo "✔ Deployment complete!"




# name: Build & Deploy to EC2

# on:
#   push:
#     branches: ["master"]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       # 1️⃣ Checkout code
#       - name: Checkout code
#         uses: actions/checkout@v3

#       # 2️⃣ Setup Java for building backend JAR
#       - name: Set up Java 17
#         uses: actions/setup-java@v3
#         with:
#           java-version: '17'
#           distribution: 'temurin'

#       # 3️⃣ Build Spring Boot JAR on GitHub runner
#       - name: Build Backend JAR
#         run: |
#           cd referral-wallet-system
#           chmod +x mvnw
#           ./mvnw clean package -DskipTests
#           echo "📦 Built JARs:"
#           ls -la target/

#       # 4️⃣ Clean old backend folder on EC2
#       - name: Clean backend folder on EC2
#         uses: appleboy/ssh-action@v0.1.10
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             rm -rf /home/ec2-user/gramflix/referral-wallet-system
#             mkdir -p /home/ec2-user/gramflix/referral-wallet-system/target

#       # 5️⃣ Upload ONLY the JAR to EC2
#       - name: Upload JAR to EC2
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           source: "referral-wallet-system/target/referral-wallet-system-0.0.1-SNAPSHOT.jar"
#           target: "/home/ec2-user/gramflix/referral-wallet-system/target/"

#       # 6️⃣ Upload backend Dockerfile (optional but recommended)
#       - name: Upload backend Dockerfile
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           source: "referral-wallet-system/Dockerfile"
#           target: "/home/ec2-user/gramflix/referral-wallet-system/"

#       # 7️⃣ Deploy using Docker Compose
#       - name: Deploy on EC2
#         uses: appleboy/ssh-action@v0.1.10
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             echo "🚀 Starting Deployment..."
            
#             cd /home/ec2-user/gramflix

#             echo "📦 Checking JAR:"
#             ls -la referral-wallet-system/target/

#             echo "🐳 Building backend docker image..."
#             docker compose -f docker-compose.prod.yml build backend

#             echo "⬆️ Restarting containers..."
#             docker compose -f docker-compose.prod.yml up -d

#             echo "✔ Deployment complete!"





# name: Build & Deploy to EC2

# on:
#   push:
#     branches: ["master"]

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       # 1️⃣ Checkout code
#       - name: Checkout code
#         uses: actions/checkout@v3

#       # 2️⃣ Setup Java for building backend JAR
#       - name: Set up Java 17
#         uses: actions/setup-java@v3
#         with:
#           java-version: '17'
#           distribution: 'temurin'

#       # 3️⃣ Build Spring Boot JAR
#       - name: Build Backend JAR
#         run: |
#           cd referral-wallet-system
#           chmod +x mvnw
#           ./mvnw clean package -DskipTests
#           echo "Built JAR files:"
#           ls -la target/

#       # 4️⃣ Clean backend folder on EC2
#       - name: Clean EC2 Backend Folder
#         uses: appleboy/ssh-action@v0.1.10
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             rm -rf /home/ec2-user/gramflix/referral-wallet-system
#             mkdir -p /home/ec2-user/gramflix/referral-wallet-system/target

#       # 5️⃣ Upload JAR to EC2
#       - name: Upload Backend JAR
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           source: "referral-wallet-system/target/referral-wallet-system-0.0.1-SNAPSHOT.jar"
#           target: "/home/ec2-user/gramflix/referral-wallet-system/target/"

#       # 6️⃣ Upload Dockerfile to EC2
#       - name: Upload Backend Dockerfile
#         uses: appleboy/scp-action@v0.1.7
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           source: "referral-wallet-system/Dockerfile"
#           target: "/home/ec2-user/gramflix/referral-wallet-system/"
#           strip_components: 1

#       # 7️⃣ Deploy using Docker Compose
#       - name: Deploy on EC2
#         uses: appleboy/ssh-action@v0.1.10
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ec2-user
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             cd /home/ec2-user/gramflix
#             echo "Building backend image..."
#             docker compose -f docker-compose.prod.yml build backend
#             echo "Restarting containers..."
#             docker compose -f docker-compose.prod.yml up -d
#             echo "Deployment completed successfully!"




name: Build & Deploy to EC2

on:
  push:
    branches: ["master"]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Setup Java for backend
      - name: Setup Java 17
        uses: actions/setup-java@v3
        with:
          java-version: "17"
          distribution: "temurin"

      # 3️⃣ Build backend JAR
      - name: Build Backend JAR
        run: |
          cd referral-wallet-system
          chmod +x mvnw
          ./mvnw clean package -DskipTests
          ls -la target/

      # 4️⃣ Build frontend (Only build - NO docker)
      - name: Build Frontend
        run: |
          cd my-app
          npm install
          npm run build
          ls -la build/

      # 5️⃣ Clean old folder on EC2
      - name: Clean EC2 folders
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            rm -rf /home/ec2-user/gramflix/referral-wallet-system
            rm -rf /home/ec2-user/gramflix/my-app
            mkdir -p /home/ec2-user/gramflix/referral-wallet-system/target
            mkdir -p /home/ec2-user/gramflix/my-app/build

      # 6️⃣ Upload backend JAR to EC2
      - name: Upload backend JAR
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "referral-wallet-system/target/*.jar"
          target: "/home/ec2-user/gramflix/referral-wallet-system/target/"

      # 7️⃣ Upload frontend build folder (static files)
      - name: Upload frontend BUILD folder
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "my-app/build/*"
          target: "/home/ec2-user/gramflix/my-app/build/"

      # 8️⃣ Upload Dockerfile.prod for frontend
      - name: Upload frontend Dockerfile.prod
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          source: "my-app/Dockerfile.prod"
          target: "/home/ec2-user/gramflix/my-app/"

      # 9️⃣ Deploy docker containers
      - name: Deploy using Docker Compose
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            cd /home/ec2-user/gramflix
            docker compose -f docker-compose.prod.yml build
            docker compose -f docker-compose.prod.yml up -d
            docker system prune -f
